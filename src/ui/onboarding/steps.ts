// ---------------------------------------------------------------------------
// 継ぐ火 -Kept Flame- 初回ガイドの内容(M57・台帳v9 必-3・UXレポート M-5 正面対応)
//
// ===========================================================================
// 1. このファイルが持つもの
// ===========================================================================
//   初回ガイド(3〜4枚のカード)の**文言だけ**。表示ロジック
//   (`OnboardingGuide.tsx`)から分離してあるのは、`RESOURCE_LABELS` 等の他の
//   語彙テーブル(`screens/contentLabels.ts`)と同じく「文言はデータ、表示は
//   コンポーネント」の分担にするため(直接テストしやすくもなる)。
//
// ===========================================================================
// 2. 文言の出典(GDD該当節・捏造禁止=検収条件)
// ===========================================================================
//   welcome     : GDD §2「一文」(コンセプト) + §3(世界観・共同体『隷』/『継ぎ手』)
//   memory      : GDD §4.2(技術は解禁→実地稼働で記憶定着→成文化で盤石の二段構え)
//   twoClasses  : GDD §7.4((A) criticalRecoverable は再取得可能・(B)
//                 rareIrreversible は一回性喪失)
//   firstStep   : ゲーム内の実際の操作導線(格子ビューへの施設配置)への誘導。
//                 GDD の特定の条文というより GDD §6.1〜6.5(配置)の要約。
//
//   **プレイヤー語彙のみ**(内部ID・GDD番号・tick生値を出さない=束B/E/Fの掃討
//   基準に従う。丸数字は `tests/ui/screens/gddNumberSweep.test.ts` が機械検証)。
// ---------------------------------------------------------------------------

export interface OnboardingStepContent {
  readonly id: string;
  readonly title: string;
  readonly body: string;
}

export const ONBOARDING_STEPS: readonly OnboardingStepContent[] = [
  {
    id: "welcome",
    title: "「継ぐ火」へようこそ",
    body: "幕塵に閉ざされた長い夜、あなたは共同体『隷』を率いる『継ぎ手』です。文明の知識は、忘れられる前に書き残せた分だけ積み上がっていきます。",
  },
  {
    id: "memory",
    title: "知識は住民の記憶に宿る",
    body: "技術は解禁しただけではまだ仮の状態です。住民が現場で実際に使うことで記憶に定着し、記録として書き残す「成文化」をして初めて確かな財産になります。書き残す前に住民を失えば、その知識も一緒に失われます。",
  },
  {
    id: "twoClasses",
    title: "技術には二つの重みがある",
    body: "基幹の技術は、たとえ失っても条件が整えばもう一度取得し直せます。焦らなくて大丈夫です。一方で希少な技術や住民の記憶は、一度失うと二度と戻りません。そちらを優先して書き残しましょう。",
  },
  {
    id: "firstStep",
    title: "まずは施設を置いてみましょう",
    body: "格子ビューで施設を配置すると、住民がそこで働き始めます。困ったときは、設定画面からいつでもこのガイドと用語ミニ辞典を開き直せます。",
  },
];

/** 最終カードの確定ボタンの文言(スキップと区別するため別の語を使う)。 */
export const ONBOARDING_FINISH_LABEL = "はじめる";

/** スキップ/各カード共通のボタン文言。 */
export const ONBOARDING_SKIP_LABEL = "スキップ";
export const ONBOARDING_NEXT_LABEL = "次へ";
