// ---------------------------------------------------------------------------
// `GridBoard.tsx` の `import "./gridBoard.css"`(副作用 import・スタイル読込)を
// tsc に許可するための最小 ambient 宣言。
//
// `vite/client` の型を丸ごと参照する手もあるが、DOM 型など無関係な宣言まで
// プロジェクト全体へ持ち込む(ADR-001 依存最小の精神に反する)ため、
// 実際に使っている「値を持たない副作用 import」の形だけをここで宣言する
// (`tools/nodeShims.d.ts` が Node 組み込みモジュールに対して取っているのと
// 同じ「使う分だけ」の方針)。
// ---------------------------------------------------------------------------

declare module "*.css";
