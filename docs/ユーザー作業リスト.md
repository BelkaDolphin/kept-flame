# ユーザー作業リスト(人間にしかできない作業)

最終更新: 2026-07-28。Claude側の実装はこれらを**待たずに進められる**(ブロックされるタスクは各項に明記)。完了したら本ファイルの該当項を消し込み、MEMORY.md に一言記録すること。

## 1. GitHub App `kept-flame-bot` のインストール確認(M45の前提・優先度: 中)

**[2026-07-28実装確認]** App 自体は作成済みで実在確認済み(`scripts/bot-token.mjs` が発行した JWT で `GET /app` を叩き `id=4415558 / slug=kept-flame-bot / owner=BelkaDolphin` を確認)。**ただし `GET /app/installations` が空配列を返しており、まだ `kept-flame` にインストールされていない**(下記手順7が未完了、または途中で終了している)。この状態だと `npm run bot:token` は「GitHub App のインストールが1件も見つかりません」で失敗する(スクリプト自体は実装・テスト・実 API 疎通確認済み)。

残作業(所要1分・すべて BelkaDolphin アカウントのまま):

1. https://github.com/settings/apps/kept-flame-bot を開く(または GitHub → Settings → Developer settings → GitHub Apps → kept-flame-bot)
2. 左メニュー **Install App** → BelkaDolphin → **Only select repositories** で `kept-flame` のみを選んで **Install**
3. 完了したら Claude に一言伝える → M45 の残り(ブランチ保護 ON・bot 作成 PR の実演・承認 merge)を再開する

**これが無いと進まないもの**: M45 の実演(bot 作成 PR)のみ。`scripts/bot-token.mjs` / `scripts/bot-pr.mjs` 本体・ブランチ保護 ON/OFF 手順・README は実装済み(他の全タスクは引き続き進行可能)。

## ~~2. 旧PATの削除(N14)~~ **[2026-07-28完了]** ユーザーが削除済み(→完了済みへ)

## 3. ②実機計測 5項目(優先度: 実機入手次第)

**目的**: 先行計測の実機待ち5項目(#1 #2 #9b #10 ほか)の判定。手順書と自己完結HTMLは準備済み。

1. 対象実機: iPhone SE2 相当 / Android 中位機 / 8GB Surface(入手・選定はユーザー)
2. 手順書: `docs/measurements/device-testing-guide.md`(配信手段×計測項目マトリクス。**iOSはヒープ計測が全経路で不可**等の制約を先に読むこと)
3. 計測ページ: `dist/device/`(壊れていたら Claude に依頼 → `npm run device:package` で再生成)
4. **最初に #10(iOS ITP 7日リテンション)を開始する** — 待ち時間が7日と最長のため、他の計測より先に着手
5. 結果(スクリーンショットや表示された JSON)を Claude に渡す → 判定と `docs/measurements/` への永続化は Claude が行う

**これが無いと進まないもの**: M19 の既定ズーム確定(#9b)と M42(実機合流)のみ。ロードマップの96%は実機なしで進行可能。

## 4. バランス調整の反復上限の承認(優先度: 低・Phase 9 = M39〜M41 の直前でよい)

GDD 13.1 が「バランス調整は各段階の追加工数上限を先に設定」と定めており、この**上限値の設定はユーザー承認事項**(ロードマップ §6.3-1)。Phase 9 に入る前に Claude から上限案を提示するので、そのときに承認・修正すること。今は何もしなくてよい。

## 完了済み(記録)

- ~~extra usage(usage credits)が OFF であることの確認~~ **[2026-07-25完了]** 課金の壁は完成
- ~~ADR承認・先行計測計画承認・配信基盤(Workers静的アセット)決定~~ **[2026-07-25完了]**
- ~~summary §6 の裁定 B1〜B9 / D1〜D7 / N系~~ **[2026-07-27完了]** 全件推奨どおりで裁定
