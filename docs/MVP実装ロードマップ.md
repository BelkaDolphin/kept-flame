# MVP 実装ロードマップ(M1〜M57)

作成日: 2026-07-27 / 入力: `docs/measurements/summary.md` §4(工数再計算)・§5(残作業)・§6(裁定済み一覧) / `docs/企画書_継ぐ火_GDD.md` §13.1・§13.2 / `docs/技術設計書_継ぐ火_ADR.md` / 書式は `docs/先行計測計画_ドラフト.md` §4 に倣う

この文書は**タスク分解と予算計画のみ**を扱う。ゲーム仕様は GDD、技術決定は ADR が正本であり、本書はそれらを実行順へ並べ替えたものにすぎない。本書の数値はすべて出典を併記し、出典のない数値は書かない。

---

## 0. 前提

### 0.1 入力となる確定事項

| 項目 | 内容 | 出典 |
|---|---|---|
| 実装ブロッカー | B1〜B9 / D1〜D7 / N1〜N13 は 2026-07-27 に**全件「推奨どおり」で裁定済み**。各設計文書へ反映済み。未処理は N14(失効 PAT 削除・ユーザー自身の GitHub 操作)のみ | summary §6.1〜§6.3 |
| 残工数 | **16.1〜17.5週**(GDD 13.1 原案 15〜17週 → 再計算 16.6〜18.0週 − 消化済み 0.5週) | summary §4.3 |
| 既存資産 | engine 4,714行 / platform 1,107行 / schema 2,587行 / sim 1,111行 / conformance 51ファイル 1,704行 / e2e 433行 / tests 8,791行。`npm test` 850件全 pass、`golden:check` 37ベクタ差分なし、`conformance:e2e` 3エンジン一致 | summary §0・§4.2 |
| 律速 | **カレンダー日数ではなく週次利用上限**。記録のある 7 タスクの実消費は計画見積り比 **3.58倍**。1 セッション(5時間枠)の実効上限 **≒ 2M トークン** | summary §4.4 |
| ②実機計測 | ~~5項目~~ **[2026-07-28] 4項目(#1 / #2 / #8 / #9b)**がユーザー実施待ち(Android/Surface は手元にあり)。**iPhone/Mac は当面入手予定なし → #7-iOS と #10 は無期限保留、M42 は4項目で判定**(残余リスク: iOS Safari 実機確認なしリリース。緩和 = ADR-004(1) の ITP 非依存設計)。判定前提(D1〜D6)は確定済み | summary §5.1・§6.2 |

### 0.2 見積り方法

先行計測計画 §4.2 と**同じ方法**で「規模(新規ファイル数 / 概算実装行数)→ 素見積りトークン」を立て、summary §4.4-1 の運用提案に従い**一律 ×3.5** を掛けた値を計画値とする。セッション換算は `×3.5値 ÷ 2,000k`(summary §4.4 の 1 セッション ≒ 2M 実効)。

- 素見積りは先行フェーズの実績レンジに合わせる(Sonnet ≒ 30k + 0.15k/行、Opus ≒ 60k + 0.16k/行、Opus→Sonnet は両者の和)。
- 先行計測計画 §4.4 と同様、**±50% の幅**と**反復ぶんの上乗せ +30〜50%**が別途乗る(§5.1)。
- 週数(GDD 13.1)とトークン(本書)は**別軸**である。週数は人間開発者の作業週、トークンは LLM エージェントの消費量であり、両者の比率は一定でない(summary §4.1)。本書は週数を「どのマイルストーンの作業か」の対応付けにのみ使う。

### 0.3 実行モデル方針

ONBOARDING §6 の流儀を維持する。**Fable5(最上位)は統率・指示出し・検分のみで、実装コードを書かない。**

| 担当表記 | 割当基準(先行計測計画 §4.1 と同一) |
|---|---|
| **Opus** | 決定論の正当性が細部に宿るもの(値域証明・区間分類・tie-break・逆CDF・二系統更新)、algoVersion / セーブ互換に触れるもの、計測境界やアルゴリズムの設計 |
| Sonnet | 参照実装からの移植、テストコード量産、UI コンポーネント、JSON content、bot ロジック、設定・スクリプト |
| **Opus→Sonnet** | 設計を Opus が確定し、量産を Sonnet が引き継ぐ |
| Fable5 | 全タスクの検分(下表「検収条件」の**検分**部分)と、タスク間の裁定のみ |

### 0.4 全タスク共通の検収条件

下表の「検収条件」欄には**タスク固有の**条件のみ書く。以下は全タスク共通で、満たさない限り完了としない。

`npm test`(全件 pass・既存テストの改変なし)/ `npm run typecheck` / `npm run lint` / `npm run format` / **`npm run golden:check` 差分なし**(差分が出た場合は ADR-016 に従い algoVersion bump の要否を裁定してからベクタを再生成)/ git ツリークリーン / Conventional Commits / **完了時に MEMORY.md へ実消費を記録**(§5.3)。

---

## 1. マイルストーン → タスクの対応

summary §4.2 の再計算後の行(新規3行を含む)を、先行計測フェーズの T0〜T16 と同じ粒度へ分解した。

| summary §4.2 のマイルストーン | 再計算 | 割当タスク |
|---|---|---|
| データ層＋決定論tickエンジン(残) | 1.5〜1.8週 | M3〜M7 |
| グローバル状態管理設計 | 0.7週 | M8 |
| 唯一保持者再取得可能性の静的グラフ解析 | 0.5週 | M9 |
| 格子UI＋施設＋タグ隣接＋配置プレビュー | 1.7〜1.8週 | M16〜M19(**裁定B5 の footprint 拡張を含む**) |
| 住民系(memoirLog・bond・寿命・想起リスク) | 1.5週 | M11〜M14(**M14 = #5 本判定の再計測**) |
| アシストアルゴリズム3種 | 1.2週 | M26・M27 |
| 探索＋冒険記＋衛星拠点(イベント10本) | 1.7〜1.9週 | M21〜M24 |
| プレステージ＋継承点＋大移動 | 0.8週 | M28 |
| UI 12画面個別工数化 | 2.2週 | M29〜M34 |
| バランス調整の段階的マイルストーン化 | 1.8〜2.0週 | M39〜M41 |
| simボット5戦略＋敵対bot6種＋夜間ゲート | 1.5〜1.6週 | M36〜M38 |
| **【新規行】conformance / golden vector の維持** | +0.5〜0.8週 | M2・M10・M15・M20・M25・M43 |
| **【新規行】設計文書(ADR/GDD)の改訂と要判断の解消** | +0.3〜0.5週 | M1 + 全タスクに分散(§3) |
| **【新規行】②実機計測の実施と対処** | +0.2週〜(不定) | M35・M42(+条件付き M42a/M42b) |
| **【本書で新設】運営基盤(ADR-021/030/015/031/022)** | (GDD 13.1・summary §4.2 とも計上なし) | **M44・M45・M46・M47・M48**(**[2026-07-27裁定]** §6.2 の裁定で MVP 内へ計上。週数の再計算は行っていない = トークンのみの追加計上) |

UI ストア実装後の #1 取り直し(summary §5.3-5 が「必須」と明記)は M35 として UI 行に含めた。

**運営基盤 5 件の週数について**: summary §4.2 は工数を「週」で再計算しているが、M44〜M48 は GDD 13.1 にも summary §4.2 にも行が存在しないため、**週数の裏付けとなる出典が無い**。本書は根拠のない週数を書かない方針(§0 冒頭)により、これら 5 件はトークン/セッションのみを見積り、週数は**未計上のまま**とする。したがって「残り 16.1〜17.5週」(summary §4.3)は本書の裁定反映後も更新していない。

---

## 2. タスク表

規模は「新規/改修ファイル数 / 概算実装行数」。`素` = 先行計測計画 §4.2 と同じ方法での素見積り、`×3.5` = 計画値、`Sess` = セッション換算(÷2,000k)。

**[2026-07-27裁定] M44〜M48 は §6.2 の裁定で後から MVP 内へ追加した運営基盤タスクである。**既存 ID の付け替えを避けるため番号は末尾連番とし、配置は依存関係に従って各 Phase へ挿入した(したがって Phase 内で ID が昇順に並ばない箇所がある)。

### Phase 0 — 前提固め(0.48 Sess)

| ID | タスク・成果物 | 担当 | 依存 | 規模 | 素 | ×3.5 | Sess | 検収条件 |
|---|---|---|---|---|---|---|---|---|
| M44 | **[2026-07-27裁定・追加] 課金の壁の preflight**(ADR-021)。(1) 週次セッション起動前に環境へ `ANTHROPIC_API_KEY` が存在すれば**起動中止**するスクリプト、(2) headless / Actions claude 連携 / Routines の workflow をリポジトリに置かないことの guardrail 検査、(3) 週次インタラクティブ消費量の概算(bisection 最大約5フルゲート再実行 + schema reject 再試行を含む)を README へ記載 | Sonnet | — | 2 / 150 | 55k | 190k | 0.10 | `ANTHROPIC_API_KEY` を仮設定した環境で preflight が非ゼロ終了する / API 連携 workflow 検査が pass / **検分**: CLAUDE.md の絶対ルール「LLM の自動実行経路を追加しない」と矛盾する経路が 1 つも無いか。ADR-021 が明示する残余(課金経路は git diff から原理的に判定不能)を README に残しているか |
| M45 | **[2026-07-27裁定・追加][2026-07-28改訂・ユーザー承認済み] ソロ運営のレビュー分離**(ADR-030 [2026-07-28改訂])。**bot identity = 運営者本人配下の GitHub App `kept-flame-bot`**(旧: 専用アカウント+PAT。fine-grained PAT が他人所有リポに発行不能な公式既知ギャップ+アカウント作成困難のため変更)。App が週次 content PR を作成 → 運営者本人が CODEOWNERS レビュアーとして承認する二役分離。ローカル保管 .pem から Node 組込み crypto で JWT 署名 → インストールトークン(寿命1h)取得 → push/PR 化するスクリプト(Anthropic API キー不使用 = M44 を侵さない・新規 npm 依存なし)、必須ステータスチェック + force-push 禁止 + 直 push 禁止 + Include administrators。**前提のユーザー作業**: App 作成(Contents/Pull requests RW のみ・`kept-flame` のみにインストール)+ .pem のリポ外保管 | Sonnet | M44 | 3 / 250 | 70k | 245k | 0.12 | bot(App)作成 PR を human が承認して merge できることを 1 回実演(admin bypass 不使用) / **検分**: .pem がリポ内・Actions secrets・`pull_request_target` のいずれにも存在しないか(ADR-021)。ADR-030(5) の「手続き的規律」但し書きがスクリプト/README に反映されているか |
| M1 | **文書残件 W1〜W4 の消化**。`authoring-procedure.md` に工程境界タイムスタンプ4点 + cond DSL 合否対比表(W1)、facility 手順へ `content/adjacency.json` 対応要否チェック(W2)、オーサリング側 6桁 floor 規則(W3)、tags-spec §9 残5件のうち実装非依存3件(W4) | Sonnet | — | 4 / 250 | 70k | 245k | 0.12 | 手順書どおりに別セッションが 1 本オーサリングして reject 0 / **検分**: W3 が `lvCurve` の 1e6 表現不能値を実際に弾くことを 1 例で確認(summary §2.5) |
| M2 | **conformance 拡張規律の設計**。entity 数が縮約盤面の十数倍になる MVP でシナリオ/ベクタをどう増やすかの方針、被覆レジストリ(`conformance/coverage.json`)の成長規則、**algoVersion bump 判定フロー**(ADR-016「golden 変化 ⟺ bump」の維持手順)を `golden-vector-spec.md` へ追記 | **Opus** | — | 2 / 200 | 90k | 315k | 0.16 | 既存 37 ベクタが 1 バイト不変 / **検分**: 「ダイジェストに algoVersion を入れない」原則(MEMORY 2026-07-25 T7前半)が拡張後も保たれるか |

### Phase 1 — データ層/エンジンの残り + 状態管理 + グラフ解析(2.07 Sess)

| ID | タスク・成果物 | 担当 | 依存 | 規模 | 素 | ×3.5 | Sess | 検収条件 |
|---|---|---|---|---|---|---|---|---|
| M3 | **セーブ層本実装**。裁定 B2〜B4 のエンベロープ `{saveFormatVersion, integrityChecksum, payload}`、migration、絶対時間/件数の強制フラッシュ、2秒デバウンス、分岐木ノード上界(ADR-012) | **Opus** | M2 | 4 / 600 | 150k | 525k | 0.26 | 旧形式セーブのロード回帰テスト / checksum 改竄検出テスト / **検分**: `eventQueueSnapshot` 非保持(B3)と `rngState` 空省略(B4)が維持されているか |
| M4 | **export/import + 容量検査 + localStorage ミラー + バックアップリマインド導線のデータ側**。ADR-012(2) の容量目標 512KB を検査値とする(裁定 D1) | Sonnet | M3 | 3 / 350 | 80k | 280k | 0.14 | export→import 往復でバイト同一 / 512KB 境界テスト / **検分**: 破損 import を黙って通さないこと |
| M5 | **rules 全系統化(1) 生産・消費・資源**。縮約3本 → 施設14種の生産式(ステータス5種・trait 倍率込み)、保管庫オーバーフロー + 廃材スポンジ + 3出口(GDD 6.7)。**裁定 B6 の正本英字ID(`forgeYield`/`efficiency`/`foodYield`)を使う** | **Opus**→Sonnet | M2 | 6 / 800 | 220k | 770k | 0.39 | 分割不変性テスト(`advance(0→T2) == advance(0→T1)+advance(T1→T2)`) / オーバーフロー損失率の算出テスト / **検分**: Math 許可リスト逸脱ゼロ、`mulFixProven` の使用箇所すべてに値域根拠のコメントがあるか |
| M6 | **rules 全系統化(2) 研究・成文化・(A)(B)二層**。E1〜E3 テック24、researchCost レンジ(GDD 12.3)、成文化キュー、**記録媒体2種(石板/紙・GDD 11.1 [2026-07-27追補]: 媒体別コスト/時間、媒体別記録の並存、E3印刷は紙のみ、balance の `recordMedia` ブロック)**。**裁定 N7 の扱いを本タスクで確定**: 「学芸3連接 → 成文化+30%」は tagMatrix(2者関係)で表現できないため MVP 対象外を維持するか、集合カウント型モデルへ拡張するかを判断し、拡張する場合は ADR/GDD 改訂 + algoVersion bump を伴うことを明記する | **Opus** | M5 | 5 / 700 | 170k | 595k | 0.30 | 全クリティカルパステックが到達可能(GDD 11.4-1)の単体テスト / **検分**: N7 の判断が文書化され、対象外維持なら GDD 6.2 注記と実装が一致しているか |
| M7 | **住民ステータス/trait の engine 反映**。裁定 B8 の正本 ID `vigor`/`dexterity`/`intellect`/`fortitude`/`will` と派生値 `combatPower` の算出式確定、trait 8種・上限3(GDD 7.2)、生産式(GDD 7.1)への結線 | **Opus** | M6 | 4 / 500 | 140k | 490k | 0.25 | `schema/engineContent.ts` のレジストリが正本 ID のみ受理し旧自由文字列を reject / **検分**: `combatPower` の式が GDD 8.2 と整合し、決定論(固定小数点)で閉じているか |
| M8 | **グローバル状態管理**。単一ストア + イベント駆動 + 派生値(adjacency computed の fan-in 上界 O(近傍)・ADR-002/ADR-028)。12画面が同一状態をリアルタイム共有。**[2026-07-28追記] 成果物に `docs/design/architecture.md`(モジュール地図・ストア形状/イベント語彙・engine⇄platform⇄UI の依存方向)を含む** — Phase 7 の画面量産セッションが M8 のコードを読み直さずに済むための1枚地図 | **Opus**→Sonnet | M5 | 6 / 700 | 200k | 700k | 0.35 | 派生値の再計算回数が更新セル近傍に限定されることのテスト / **検分**: 構造共有の単一正準実装(ADR-028)から外れた複製経路が増えていないか |
| M9 | **唯一保持者再取得可能性の静的グラフ解析** `graph.ts`。prereq グラフ上で各 (A) テックの fieldRequirement を満たす role が常時 ≥1人 再構成可能かを探索(GDD 11.4-2) | **Opus** | M6 | 3 / 450 | 130k | 455k | 0.23 | ソフトロック盤面を人工的に作って検出されることのテスト / **検分**: 探索順が決定論(ID 昇順)で固定されているか |
| M10 | **conformance 拡張 #1**(engine 全系統化ぶん)。シナリオ追加 + ベクタ再生成 + 被覆レジストリ更新 + algoVersion 判定。**[2026-07-29追記] 記憶巧者 trait ID の統一を含む**(base content を `traitMemoryKeeper` に・sc09 patch の冪等化・`balance.recallRiskParams.memoryKeeperTraitId` 結線。M7 の暫定 `traitLivingLibrary` を置換) | Sonnet | M2,M7,M9 | 4 / 400 | 90k | 315k | 0.16 | `golden:check` 差分なし / `checkCoverage` 空配列 / `conformance:e2e` 3エンジン一致 / **検分**: 新経路が `observedBy` で正直に分類されているか |
| M49 | **[2026-07-29追記] engine コマンド層 `src/engine/commands.ts`**(M8 の発見: プレイヤー操作を engine に適用する入口の担当タスクが無かった)。配置/解体/増築/住民割当/成文化指示/派遣確定等を単一入口 `apply(Command)` に集約、ADR-012 の分岐木カウントと saveScheduler のコマンドトリガに結線、ストアの暫定イベント `stateApplied` を撤去(architecture.md §7-2 の手順) | **Opus** | M7,M8 | 3 / 400 | 110k | 385k | 0.19 | 全 state 変更がコマンド経由であることのテスト / `stateApplied` の完全撤去 / **検分**: コマンドが engine 純関数で、UI 側に判定ロジックが漏れていないか |
| M50 | **[2026-07-30裁定・追加] コマンド層残件**(M49 完了時に判明した2欠落)。(1) **`beginResearch`(研究対象選択)**: 現行の縮約「未完了 ID 昇順先頭」を解消する。**golden 変動 = algoVersion bump を伴う**ため、実施時は他の bump 案件と同一コミット群に束ねて bump を 1 回で済ませる(spec §9 の差分規律に従う)。(2) **建設/増築コストの支払い結線**: 裁定によりコスト項は **facility スキーマ側**(GDD 12.1 [2026-07-30裁定] 参照。省略可+ローダー必須の二段構え)。`buildFacility`/`upgradeFacility` が資源を払い、廃材3出口(1) 建設代替20%(GDD 6.7)の呼び出し元を接続する。**[2026-07-31裁定・台帳v6 必-1] 「engine配線・コマンド補完の束ね」へ拡張**(M31/M32 完了時に具体化した未配線5件を1タスク・1検証で実施): (3) **成文化の scheduler 結線**(M6 以来の積み残し。`PIPELINE_STAGE.codify` に `applyCodifyProgress`/`completeCodification` を接続し、キュー投入した記録が時間経過で完了するように。conformance ベクタで挙動固定) (4) **成文化キューの取消コマンド新設**(additive) (5) **衛星拠点の操作コマンド新設**(駐在割当/解除・設置/放棄=⑨画面の表示専用解消)。bump 要否は実測判定(M25 前例: 既存ベクタが動かなければ bump 不要)。**実施は M28/M34 統合後の次ラウンド優先**(本タスクで研究→成文化→拠点の本流ループが通しで回る) | **Opus** | M49,M6 | 5 / 650 | 170k | 595k | 0.30 | コスト不足コマンドの機械可読 reject / 研究選択がセーブ往復で保持される / bump 判定が spec §9 の「既存ベクタ expected 変化のみ」規律に従っている / **検分**: bump が他案件と束ねられているか、コスト無払い経路が残っていないか |

### Phase 2 — 住民系(0.89 Sess)

| ID | タスク・成果物 | 担当 | 依存 | 規模 | 素 | ×3.5 | Sess | 検収条件 |
|---|---|---|---|---|---|---|---|---|
| M11 | **住民寿命モデル + 人口下限保証 + 獲得/規模**(GDD 7.5〜7.7)。「唯一保持者残存想定tick」を機械算出できる形にする(GDD 11.4-4 の前提) | **Opus** | M7 | 4 / 500 | 140k | 490k | 0.25 | 人口が全tickで `min(寝床×0.5, 6)` 以上(GDD 11.4-9)のテスト / **検分**: 残存tick算出が寿命モデルの純関数として取り出せているか |
| M12 | **memoirLog + bond + 記憶可視化のデータ層**(GDD 7.3)。決定論テンプレのエピソード生成 | Sonnet | M11 | 4 / 450 | 100k | 350k | 0.18 | 同一 seed で memoirLog がバイト同一 / **検分**: テンプレ選択に Map 反復順依存が無いか |
| M13 | **想起リスクフックの本実装**。縮約(住民単位停止)→ 本式(tech 別停止・mastery・(B)一回性喪失)。`p_step = p_day × coarse/1440` の線形按分は裁定 N1 のまま維持。**[2026-07-30裁定] M12 の memoir/bond tick 結線(純関数は M12 で用意済み)を本タスクに含める**(recall 結線と同時に行う) | **Opus** | M12 | 3 / 450 | 130k | 455k | 0.23 | 分割不変性テスト / GDD 11.7 の段22(回復)/段24(抽選)位置が裁定 B1 のまま不変 / **検分**: mastery 上限 0.20 の clamp 挙動(N12)が仕様どおりで、後半の減衰が意図的なものとして残っているか |
| M14 | **#5 本判定の再計測**(裁定 B9)。`sim/recallFrequency.ts` を本実装の住民系で再実行し、**盤面平均**で 1〜3回/住民/週 を判定。結果 JSON を `docs/measurements/` へ保存(付録 A-4 の教訓) | Sonnet | M13 | 1 / 150 | 60k | 210k | 0.11 | 結果 JSON が `docs/measurements/` に永続化されている / 8 seed で決定論一致 / **検分**: 判定単位が盤面平均であること(B9)、パターン別の下限割れをバランス知見として別記していること。**[2026-07-30裁定]** 判定ペアの縮約(全生存住民×全 research techId・保持者に絞らない)の再評価も本タスクで行う(M13 予備値: 2.825回/住民/週=レンジ内) |
| M15 | **conformance 拡張 #2**(住民系ぶん)。**[2026-07-30裁定]** M13 の本式化で sc06 が「想起困難で生産が止まる」経路を観測しなくなった(発生 tech の実地要件施設に就労者がいないため)→ **実地要件施設に就労者を置くシナリオを必ず追加**(反証義務: 停止で digest が動くことを数値確認) | Sonnet | M10,M13 | 3 / 350 | 80k | 280k | 0.14 | M10 と同じ | 

### Phase 3 — 格子UI + 施設 + タグ隣接(1.00 Sess)

| ID | タスク・成果物 | 担当 | 依存 | 規模 | 素 | ×3.5 | Sess | 検収条件 |
|---|---|---|---|---|---|---|---|---|
| M16 | **footprint 2×1 / 2×2 の state 拡張**(裁定 **B5**: GDD 仕様を維持し本行で実装)。占有セル集合の保持と**基準セル導出**(GDD 6.1・6.3) | **Opus** | M5 | 3 / 400 | 125k | 440k | 0.22 | 大型施設の占有/衝突/撤去の単体テスト / **検分**: 基準セル導出が決定論で、既存の全施設1×1セーブがマイグレーション可能か |
| M17 | **タグ隣接の全系統化**。複数タグ過密の独立集計、大型施設の判定基準、±60% クランプ(裁定 N2 = engine 定数のまま)、tagMatrix キーは `tag\|tag` のみ・施設指定は `target` 側(裁定 N8)。**[2026-07-30裁定・最重要申し送り] 隣接判定は state の footprint を読むこと(content 非権威)**。content 側を権威にすると既存 golden シナリオの forge(content 上は 2×1)が 2 セル占有になり **40本全再生成 + bump が発生する**。adjacency.ts §3(e) の「T5 では全施設 1×1」コメントの更新も本タスク | **Opus** | M16 | 3 / 450 | 130k | 455k | 0.23 | 過密ペナが独立集計されることのテスト / **検分**: GDD 6.3(c) の辞書順選抜が裁定 N10 の文言どおり「決定論のため常にセルID辞書順」として残っているか |
| M18 | **格子UIコンポーネント**。6×8 + 瓦礫開墾、ピンチズーム/パン、最小タップ領域 44px、タップ選択→配置先タップの2ステップ(GDD 6.6)。**[2026-07-30追記・M17申し送り]** `cellPlacement[]` は全占有セルに載る(非アンカーからも施設が引ける)。描画は `anchorCellIndex === cellIndex` のセルにのみ枠/バッジ/数値、残りは連結表示。`cellSelected` のアンカー正規化の要否は本タスクで判断(★候補) | Sonnet | M8,M17 | 5 / 600 | 120k | 420k | 0.21 | 240 DOM の mount 時間が予算 200ms 内(ADR-029) / **検分**: 非アクティブ画面アンマウント方針(ADR-027)と矛盾しないか |
| M19 | **配置プレビュー + 4重符号化**。`docs/design/tags-spec.md` の機械可読 JSON を実装へ落とす(色/記号/パターン/数値)、常時過密警告バッジ、内訳ビュー、凡例パネル。**既定ズーム倍率は #9b(実機)確定まで暫定値を定数1箇所に置く**(§4.4) | Sonnet | M18 | 5 / 600 | 120k | 420k | 0.21 | `npm run measure:contrast` 68行全 pass(パレットを触った場合は必須・summary §2.4) / **検分**: 既定ズームが定数1箇所に隔離され、#9b 結果で差し替えるだけで済む形か。**[2026-07-30裁定]** 過密バッジの engine 値 `overcrowdedNeighborCount` は「(タグ,施設) の超過ペア数」であって施設数ではない — バッジにそのまま出すかは表示側判断とし、内訳ビューでタグ別に分解すること |
| M20 | **conformance 拡張 #3**(footprint / 過密ぶん)。**[2026-07-30追記・M17申し送り]** 現行 golden 56本には大型施設が1基も無い → 2×1 / 2×2 シナリオを必ず追加。反証素材は `tests/engine/adjacencyFootprint.test.ts` の数値(2×1@8 の基準セル10個 / 重複除去 0.2 vs 0.4 / クランプ 0.8 vs 0.2)がそのまま使える。`foot-` 接頭辞の coverage 登録も本タスク | Sonnet | M15,M17 | 3 / 300 | 75k | 265k | 0.13 | M10 と同じ | 
| M52 | **[2026-07-31裁定・追加] 瓦礫 state 化 + 開墾**(M18 完了時に判明した欠落のタスク化)。GameState への地形/瓦礫の追加(縮約互換=瓦礫ゼロが既定で **golden 64本不変**を成立させること)、開墾コスト式 `base×1.15^解放数`(GDD 9.1)、`commands.ts` の型予約 `reclaimCell` の実装。初期盤面の瓦礫配置は content/balance 側の定義とセット。GridBoard 側の UI 導線は M30 が薄く足す(M18 が呼ぶだけの形に設計済み) | **Opus** | M16,M49 | 3 / 400 | 120k | 420k | 0.21 | 瓦礫セルへの配置/増築が reject される / 開墾コストが**通算**解放数の 1.15 冪で増える([2026-07-31裁定 B-1] GDD 9.1 が正・当初の「エラ内で」は誤記) / 旧セーブ(瓦礫フィールド無し)が「全セル開墾済み」として無損失ロードされる / **検分**: saveSchemaVersion bump 要否が ADR-012 の線引きで明示判断されているか |

### Phase 4 — 探索 + 冒険記 + 衛星拠点(1.21 Sess)

| ID | タスク・成果物 | 担当 | 依存 | 規模 | 素 | ×3.5 | Sess | 検収条件 |
|---|---|---|---|---|---|---|---|---|
| M21 | **探索エンジン**。派遣枠2、距離帯 **`near`/`far`/`deep`**(裁定 **B7**)、決定論解決(GDD 8.2)、全滅リスクと安全曲線、ROI に (B) 喪失を金銭化(GDD 8.6) | **Opus** | M11,M13 | 5 / 650 | 165k | 580k | 0.29 | ROI が距離帯ごと 1.2〜2.0 レンジ(GDD 8.6)に収まるテスト / **検分**: 逆CDF が段階1(独立系)に留まり ADR-018(3) の段階2へ踏み込んでいないか |
| M22 | **event ランタイム**。cond DSL 評価器(jsep AST ホワイトリスト・GDD 12.2)、choices 分岐、判定前の質的分岐(GDD 8.3)、item overflow、**効果プリミティブ `destroyRecords{medium, scope}`(GDD 11.1 追補・MVP では content 側未使用のまま conformance ベクタで挙動固定)**。既存 `schema/event.ts`(487行・engine 非接続)を engine へ結線。**[2026-07-30追記・M21申し送り]** ①ノード生成の差し替え点は `buildDispatchSnapshot` 1箇所(木の形を変えるなら `persistence.ts` の `DISPATCH_TREE_CHILD_KEYS` 確認) ②帰還ログの content `logTemplate` 結線は `renderReturnLog` 差し替え ③`schema/event.ts` の `statWeights` 正本語彙への締めと `destroyRecords` 実装 ④**晴天漂着への memoir bio 結線**(裁定★5: 保護加入は `initializeResidentMemoir` 済み・晴天漂着が未結線=M15 の `mem-bio-arrival-unwired` の解消)を本タスクに含める | **Opus**→Sonnet | M21 | 5 / 600 | 180k | 630k | 0.32 | ホワイトリスト外演算子の reject テスト / **検分**: DSL 評価が固定小数点で閉じ、浮動小数を 1 度も通らないか |
| M23 | **event content 10本**(近郊3/遠隔3/深部4・平均5ノード ≒ 50ノード・GDD 8.7)+ 冒険記テンプレ。裁定 **B6〜B8 の正本英字ID で投入**。**[2026-07-27] #12 再測定でプレースホルダ解消**(§6.1)。**[2026-07-31裁定・オーサリング規律]** GDD 12.1/12.2 の [2026-07-31裁定] ブロックが正本: `result` はオブジェクト形正本+短縮4語のみ / **最後の branch は cond リテラル `true` 必須** / logTemplate はプレースホルダ8種のみ / statWeights は B8 正本5語(ContentBundle 経由 strict で機械強制) | Sonnet | M22 | 11 / 900 | 165k | 580k | 0.29 | schema 検証 reject 0 / 全ノードが sim で到達(GDD 11.4-10) / **検分**: 距離帯・ステータス ID が正本(B7/B8)で書かれているか |
| M24 | **衛星拠点3タイプ + ROI 表示のデータ層**(GDD 9.2) | Sonnet | M21 | 4 / 450 | 100k | 350k | 0.18 | 拠点網 ROI のテスト(GDD 11.4-7) / **検分**: 本拠と拠点で資源系の二重計上が無いか |
| M25 | **conformance 拡張 #4**(探索 / event / outpost ぶん)。**[2026-07-31裁定・bump 束ね]** 晴天漂着への memoir bio 結線(M15 の `mem-bio-arrival-unwired`・パッチは `rules/population.ts` 2箇所と実測済み)を本タスクで実施し、**algoVersion 2→3 と sc24 系 golden 3本の再生成をベクタ拡張と同一コミットに束ねる**(単独 bump を発生させない)。**[2026-07-31裁定・追加束ね(台帳v2 必-1)]** M24 が用意した `applyOutpostSupply` の **scheduler 段80 への結線**も本タスクで実施し、sc-outpost 系ベクタで挙動を固定する(既存 golden シナリオに拠点ゼロのため結線自体は digest を動かさない見込み・要実測確認) | Sonnet | M20,M23,M24 | 4 / 450 | 100k | 350k | 0.18 | M10 と同じ / bio 結線後に `mem-bio-arrival-unwired` が解消されている / 段80 結線後に outpost 供給がベクタで観測されている | 

### Phase 5 — アシストアルゴリズム3種(0.36 Sess)

| ID | タスク・成果物 | 担当 | 依存 | 規模 | 素 | ×3.5 | Sess | 検収条件 |
|---|---|---|---|---|---|---|---|---|
| M26 | **推奨配置(貪欲・理論最大の80%を狙う)**(GDD 13.1 / 2.1) | **Opus** | M17 | 2 / 350 | 115k | 400k | 0.20 | 代表盤面で総当たり最適と比較し 80% 前後に収まるテスト / **検分**: 貪欲の局所最適が理論最大に近づきすぎていないか(GDD 14-3 の残余リスク) |
| M27 | **おまかせ成文化(残存tick降順80%)+ 探索テンプレ(戦力充足80%)**。**[2026-07-31裁定 A-7]** M52 の瓦礫セルを推奨配置の `blockedCells` へ自動で流し込む薄いヘルパを assist 側に置く作業を本タスクに含める(呼び出し側任せの呼び忘れ防止) | Sonnet | M13,M21,M26,M52 | 3 / 400 | 90k | 315k | 0.16 | 同上の 80% 検証 / **検分**: 3種とも同じ「80/100」の尺度で測られているか |

### Phase 6 — プレステージ(0.29 Sess)

| ID | タスク・成果物 | 担当 | 依存 | 規模 | 素 | ×3.5 | Sess | 検収条件 |
|---|---|---|---|---|---|---|---|---|
| M28 | **大移動 + 2プールナップサック + 周回シード導出 + 継承点**(GDD 10.2〜10.5) | **Opus** | M11,M13,M21 | 5 / 650 | 165k | 580k | 0.29 | 周回シード導出が同一入力でバイト同一(GDD 10.5) / 継承点の上限クランプで青天井にならない(GDD 11.4-6) / **検分**: 2プール競合の解決順が決定論か |

### Phase 7 — UI 12画面 + PWA(1.46 Sess)

| ID | タスク・成果物 | 担当 | 依存 | 規模 | 素 | ×3.5 | Sess | 検収条件 |
|---|---|---|---|---|---|---|---|---|
| M29 | **アプリシェル + ルーティング + ①ホームハブ + ⑫帰還ダイジェスト**。**[2026-07-28追記] 着手時に最初に `docs/design/ui-spec.md`(12画面の表示要素・遷移・GDD該当節の対応表のみの軽量仕様)を作成**(§6.3-2「UI単価は実績ゼロ」への対策・ワイヤーフレームは作り込まない)。自前ハッシュルータ・非アクティブ画面アンマウント(ADR-027)、緊急度バッジからのワンタップ遷移(GDD 6.6)、tick をフォアグラウンド単調時刻の純関数に(ADR-026)。**[2026-07-30裁定]** ui-spec 作成時に `GridSummary.overcrowdedCellCount`(実態は過密している施設数=アンカーのみ集計)の `overcrowdedFacilityCount` への命名整理を含める | **Opus**→Sonnet | M8 | 7 / 700 | 200k | 700k | 0.35 | 画面遷移でストアが再生成されないテスト / **検分**: タイマー発火回数に依存する経路が 1 つも無いか(ADR-026) |
| M30 | **②格子ビュー / ③施設詳細・増築 / ④住民一覧・配置** | Sonnet | M18,M19,M29 | 6 / 700 | 135k | 470k | 0.24 | 44px 最小タップ領域の自動検査 / **検分**: M18/M19 の再実装でなく再利用になっているか |
| M31 | **⑤研究ツリー / ⑥成文化キュー** | Sonnet | M29,M6 | 5 / 550 | 115k | 400k | 0.20 | 同上 / **検分**: (A)(B) 二層の区別が UI 上で判別可能か(GDD 7.4) |
| M32 | **⑦探索本部 / ⑧冒険記ビューア / ⑨衛星拠点管理** | Sonnet | M29,M23,M24 | 6 / 700 | 135k | 470k | 0.24 | 同上 / **検分**: ROI の (B) 損失項が画面に出ているか(GDD 8.6) |
| M33 | **⑩大移動ナップサックUI / ⑪継承点購入 / ＋セーブ・設定**。**[2026-07-31追記・台帳v5 追-5]** 通常セーブの結線は M29 が前倒しで実装済み → 本タスクの残りは**破損セーブの救済導線と export/import UI** | Sonnet | M29,M28,M4 | 5 / 600 | 120k | 420k | 0.21 | export/import が UI から往復できる / **検分**: バックアップリマインド導線が大移動・継承点獲得時に出るか |
| M34 | **PWA Add-to-Home 誘導 + 通知の条件分岐**(GDD 13.3)。ITP 非依存設計(ADR-004(1))は既決なので、通知が実機で機能する条件が満たせなければ代替リテンションのみで組む分岐を実装 | Sonnet | M29 | 3 / 300 | 75k | 265k | 0.13 | 通知不可経路でも起動・復帰が壊れないテスト / **検分**: バナー判定が最終起動 monotonicTimestamp のみに依存しているか |
| M35 | **#1 の取り直し**(実 UI ストアでの B3 再計測)。summary §5.3-5 が「UI 実装後に取り直し必須」と明記。`hydrateFidelity: "placeholder"` を実値へ差し替え。**cold/warm 両方を併記**(裁定 D2) | Sonnet | M33 | 1 / 150 | 55k | 190k | 0.10 | 結果 JSON を `docs/measurements/` へ保存 / **検分**: `isOfficialVerdict` の扱いがデスクトップ値=非公式のまま正しいか |
| M54 | **[2026-08-01裁定・台帳v8 必-1 + v9 必-1・追加] UI 残件束ね**。engine 側は M50 で完成済みだが画面側が未接続の 4 件 + 脱出導線 1 件: (1) ⑥成文化キューの取消ボタン(`cancelCodification` 結線) (2) ⑨衛星拠点の操作 UI(設置/放棄・駐在の割当/解除の 4 コマンド結線) (3) 定期バックアップ推奨バナーの配線(現状は大移動直後のみ) (4) 起動時にセーブ破損をその場で知らせる導線(現状は設定画面の常設フォームのみ) (5) **設定画面の「最初からやり直す」導線**(確認 2 段つきの `newGame` 再実行・v9 必-1 でスコープ追加)。engine・golden vector には触れない画面のみの作業 | Sonnet | M50,M33,M53 | 3 / 350 | 90k | 315k | 0.16 | 5 件すべてが実ブラウザ操作で通る(Playwright) / **検分**: engine/schema/content/conformance 差分ゼロか |
| M55 | **[2026-08-01・事後記録] UX シェル改修(束A)** — UX プレイテスト(fatal 5/major 8/minor 9)を受けた飛び込み実装の台帳登録。sticky ヘッダ+資源 HUD / ナビ 5 グループ化 / 格子ボード実寸キャップ(55vh)+2 カラム / タイポトークン 12px 床 / 研究エラアコーディオン / sticky 確定バー。**完了済み**(Opus・コミット `5c6ee88`・実消費 289k) | Opus | M30〜M33 | — | — | 289k | — | (完了済み) 12px 未満 0 件の実測 / ナビ構造追随テスト |
| M56 | **[2026-08-01・事後記録] 文言・フィードバック改修(束B)** — 同上の台帳登録。拒否コード 32 種の日本語マッピング(engine 定数との突合テスト付き) / 成功トースト 5 種 / カタログ・研究・開墾のコスト表示 / event 和名 10 件+住民表示名 / 開発メモ・内部 ID の全掃討。**完了済み**(Sonnet・コミット `67ba921`・実消費 691k) | Sonnet | M30〜M33 | — | — | 691k | — | (完了済み) 13 画面の機械検証 0 件 / 拒否コード網羅の突合テスト |
| M57 | **[2026-08-01裁定・台帳v9 必-3・追加] オンボーディング(初回ガイド + 用語ミニ辞典)**。初めて遊ぶ人向けの目的提示(「知識を書き残して火を継ぐ」)と用語解説((A)/(B)・想起困難・成文化・大移動等)。UX レポート M-5(major)への正面対応。最低限の画面サブタイトルは束B で実装済み、本タスクは初回フロー+辞典画面。着手は M54 後を推奨 | Sonnet | M53 | 4 / 400 | 90k | 315k | 0.16 | 初回起動でのみガイドが出る(セーブ復帰では出ない) / **検分**: 用語説明が GDD の定義と矛盾しないか |

### Phase 8 — 運営 guardrail + sim ボット + 夜間ゲート(0.92 Sess)

| ID | タスク・成果物 | 担当 | 依存 | 規模 | 素 | ×3.5 | Sess | 検収条件 |
|---|---|---|---|---|---|---|---|---|
| M46 | **[2026-07-27裁定・追加] guardrail と正準パイプラインの結線**(ADR-015)。(1) content-diff-gate に **checkout 時の symlink follow 禁止 + 通常ファイル型限定チェック**(非通常ファイルは全面 reject)、(2) 正準順序 `schema検証 → canonicalize → id-registry → 段階sim1000 → content-diff-gate → conformance 3ブラウザ → 統計クリープ検出 → bot PR human merge → beta ソーク → 本番` を CI 記述と一致させる。段階 sim1000 と統計クリープ検出の**実体は M38 で差し込む**ため、本タスクではジョブ定義と順序の固定までを行う | Sonnet | M10,M45 | 4 / 400 | 90k | 315k | 0.16 | content/ 配下に置いた symlink が reject されることのテスト(実体が `.github/workflows/` を指す例) / content-only PR で tsc/Vitest が goldenVector 検証のみにスコープされる / **検分**: ADR-015 の正準順序と workflow の実記述が 1 段もずれていないか |
| M51 | **[2026-07-30裁定・追加] additive 意味論 diff の機械強制**(M46 完了時に「未実装」と正直申告された残件)。content PR に対し「既存 ID の削除/リネーム/意味変更の reject・tombstone 逆参照チェック(GDD 12.5-2/3)」を git diff ベースで機械判定するスクリプトを `scripts/` に追加し、guardrail の content-diff-gate 段へ結線する。**[2026-07-30裁定]** CODEOWNERS 自己保護行は承認済み・status checks は ADR-030(4) の個別命名を維持・PLACEHOLDER 2 ジョブは M38 まで required checks に含めない運用 | Sonnet | M46 | 2 / 250 | 60k | 210k | 0.11 | 既存 ID を削除する PR・意味変更(型/レンジ逸脱)する PR が reject されるテスト / tombstone を参照し続ける PR が reject される / **検分**: additive 判定が git diff の構文でなく JSON 意味論(canonicalize 後比較)で行われているか |
| M36 | **5戦略bot**(貪欲/研究優先/探索優先/配置戦略違い/成文化優先)。**全 bot に閾値付き想起リスク判断を実装**(GDD 11.5・ラベルだけの bot にしない)。**[2026-08-01裁定・台帳v7]** 大移動 bot の出発点には `recommendExodusPlan`(M28・正式アシスト外の貪欲)を使ってよい。**GDD 11.4-6「全系統上限到達 10〜18周」の sim 実証は M36〜M38 の担当**(engine 側の上限 4段/407点/系統は M28 で固定済み) | Sonnet | M13,M21,M23,M26,M27 | 6 / 700 | 135k | 470k | 0.24 | 同一 seed 1000回が完全一致(GDD 11.4-5) / **検分**: 「未成文の唯一保持技術を持つ住民は士気<40 で過酷業務・派遣に回さない」等の判断が実際に踏まれているログ証跡 |
| M37 | **敵対bot 6種**(a〜f・GDD 11.6)。クロック前進 exploit 再現 bot(f)は GDD 11.9 のレート制限を検査する | Sonnet | M36 | 6 / 650 | 130k | 455k | 0.23 | 6種すべてが低頻度エッジを決定論的に強制発生させることの確認 / **検分**: (f) がレート制限を実際に発火させているか |
| M38 | **夜間ゲート整備 + 11条件 assert + 再校正**。GDD 11.4 の 11 条件をすべて不等式 assert 化、カバレッジ/頻度 assert、**bot 最大11本での `measuredSecPerRun` 再校正**(ADR-014(3)・summary §2.1 が「再校正の義務は消えない」と明記)。M46 が固定したパイプラインの「段階 sim1000」「統計クリープ検出」の枠へ実体を差し込む | **Opus**→Sonnet | M9,M37,M46 | 4 / 500 | 170k | 595k | 0.30 | 実 ubuntu-latest runner で週次 11000 runs が 30分目標内(`.github/workflows/calibrate.yml` を再実行) / **検分**: 11 条件すべてが不等式で、bool 判定に化けていないか。Fallback ①〜④ の発動要否を再判定(summary §3.1) |

### Phase 9 — バランス調整(0.82 Sess)

各段階の**追加工数上限を先に設定**してから着手する(GDD 13.1 の当該行の指定)。反復回数が読めないため、下記は 1 段階あたり 1〜2 反復を前提とした計画値である。

| ID | タスク・成果物 | 担当 | 依存 | 規模 | 素 | ×3.5 | Sess | 検収条件 |
|---|---|---|---|---|---|---|---|---|
| M53 | **[2026-07-31裁定・台帳v5 必-1・追加] ニューゲーム生成 + 難度シード**(M29 完了時に判明した欠落のタスク化)。初期状態の正式生成: worldSeed 入力 → 初期住民数名の seed 決定論生成(寿命 §7.5・trait・加入時年齢)、根拠ある初期資源、初期盤面(M52 の `initialRubbleCells` と整合)、難度シード**「穏」**(GDD 2.2: 想起困難頻度 0.5倍・(B)出現頻度低)の選択。M29 暫定の `src/newGame.ts`(worldSeed 固定/住民 life 無し/初期資源仮数)を置き換える。**[2026-08-01裁定・台帳v7 必-2] 大移動後の新周回の開始状態(初期施設配置・大移動で持ち込んだ資産の展開)の生成も本タスクの担当**(初回起動と同じ生成器を通す。現状の M28 は施設ゼロの更地で次周を開始する)。**M39(バランス調整)着手前に完了必須**(校正が仮初期状態に依存しないため) | Sonnet | M11,M52 | 3 / 350 | 90k | 315k | 0.16 | 同一 worldSeed でニューゲーム生成がバイト同一 / 初期住民全員が life を持つ / 難度「穏」で該当係数が反映される / **検分**: golden 73本不変(ニューゲーム生成が conformance シナリオと独立か)・生成が engine 決定論規則(ADR-006/007)内か |
| M39 | **E1 到達収束**。指数コスト vs 線形供給(GDD 5.1)の調整、`content/balance.json` の係数のみ変更。**[2026-07-30裁定]** M14 で据え置いた想起判定ペアの縮約(全生存住民×全research techId)と masteryGain 定数を、裁定 N12 と併せて M39〜M41 の再評価対象に含める。**[2026-07-31裁定・台帳v2]** M23 の event 10本の difficulty/R(到達検証成立を優先した設計値・balance 帯より低め)と M24 の outpost 距離帯維持費係数(near 1.0/far 1.4/deep 1.8 暫定)も再評価対象に含める。**[2026-08-01裁定・台帳v7]** M28 の `balance.exodus`(expectedTabletsByEra/inheritBonusPerTier=根拠なしの暫定)と M34 の誘導バナー頻度定数(猶予24h/再表示7日/上限5回)も再評価対象。**[2026-08-01裁定・台帳v8/v9]** さらに以下を再評価対象に追加: ①**開始資源の量**(初期薪60に対し建設30+開墾40でギリギリ・M53 の floor は開墾1回分ちょうど・v9 必-2) ②**大移動の実行ガード**(継承点ほぼゼロでの大移動は新周回が極端に貧しくなる・現状は警告のみで実行可能・v9 必-2) ③**拠点の設置コスト**(現状ゼロ・導入するなら content 項目追加・v8 追-2) ④**M53 の暫定数値**(初期住民6名・trait 0〜2個の非復元抽選・v9 追-4) ⑤**学芸タグの成文化加速(codifySpeed)の適用要否**(裁定N7=MVP対象外のままだが成文化実装済みの今が再検討の入口・v8 記録) | **Opus** | M38 | — / 調整 | 150k | 525k | 0.26 | 夜間ゲート全 assert pass / **検分**: engine コードを触らず content 側だけで収束したか(触ったなら algoVersion 判定へ) |
| M40 | **E2 段階** | **Opus** | M39 | — / 調整 | 150k | 525k | 0.26 | 同上 / **検分**: era 移行後 N tick 窓の実効研究速度低下が閾値内(GDD 11.4-7) |
| M41 | **E3 段階 + 周回健全性**。E3 到達 8〜16日相当(GDD 11.4-3)、全系統上限 10〜18周レンジ(GDD 11.4-6) | **Opus** | M40 | — / 調整 | 170k | 595k | 0.30 | 同上 + 周回 assert / **検分**: 上限クランプで青天井にならないこと |

### Phase 10 — 実機合流・配信基盤・最終検分(0.72 Sess)

| ID | タスク・成果物 | 担当 | 依存 | 規模 | 素 | ×3.5 | Sess | 検収条件 |
|---|---|---|---|---|---|---|---|---|
| M42 | **②実機計測結果の反映**。#1/#2/#8/#9b/#7-iOS の結果 JSON を `docs/measurements/` へ収容し、判定を summary へ追記。#9b の `minLegibleSizePx` で M19 の既定ズームを確定。裁定 **N13**(ADR-029 可変ドラフトの要否)を #2 実機ヒープで確定 | **Opus** | ②実機実施, M35 | 2 / 250 | 120k | 420k | 0.21 | 結果 JSON が永続化され判定が summary に載る / **検分**: cold/warm 併記(D2)、GCポーズは保守側 0.83ms 代替(D4)、iOS ヒープは間接指標(D5)という裁定どおりの読み方をしているか |
| M47 | **[2026-07-27裁定・追加] stable/beta 昇格機構**(ADR-031・ADR 自身が「MVP へ前倒し」と明記)。(1) `wrangler.toml` の `[env.stable]` / `[env.beta]` を**別 Worker**(`kept-flame-stable` / `kept-flame-beta`・別サブドメイン)として定義(= Workers 静的アセット配信設定そのものを含む)、(2) 週次 content はまず beta へデプロイ、(3) **soak N=3日**かつ「beta で P0/エラー閾値超なし」を日付/署名チェックで機械判定してから stable 昇格を許可(未満は block)、(4) 最終 promotion は人間の手動操作 | Sonnet | M34,M46 | 4 / 350 | 85k | 300k | 0.15 | `wrangler deploy --env beta` / `--env stable` が別 Worker・別サブドメインへ出る / 日付を偽装した dry-run で soak 3日未満の promotion が block される / **検分**: ADR-031(5) のとおり自動ロールバック・KV last-known-good・ヘルスビーコンを持ち込んでいないか(YAGNI 整合)。ADR-025 の凍結アーカイブへ踏み込んでいないか(§6.2) |
| M48 | **[2026-07-27裁定・追加] リリース後監視の最小結線**(ADR-022)。(1) ヘルスビーコンは未認証の生カウンタ送信をやめ、配布時に埋めた回転鍵で **HMAC 署名 + Cloudflare 側レート制限 + 送信元検証**、(2) **ビーコン単独では自動ロールバックを起動させない**(「ビーコンのエラーレート閾値超」かつ「Sentry 新規エラー署名 または ゲーム内報告」の 2 独立信号照合を必須)、(3) beta もロールバック対象、(4) last-known-good を Workers KV に明示記録 | Sonnet | M47 | 3 / 300 | 75k | 265k | 0.13 | 未署名/署名不正のビーコンが reject される / 1 信号のみではロールバックが起動しないことのテスト / **検分**: 公開クライアントゆえ鍵抽出が原理的に可能という残余リスクが文書に残り、それを単独トリガ禁止で限定する構造になっているか |
| M43 | **最終 conformance + algoVersion 確定 + 全体検分**。MVP 全 entity でのシナリオ/ベクタ最終拡張、3エンジン一致の再確認、algoVersion の確定 bump | **Opus** | 全 | 3 / 300 | 130k | 455k | 0.23 | `golden:check` / `conformance:e2e` / `npm test` / `measure:contrast` / `sim` 夜間ゲート すべて pass / **検分**: ADR-016 の「golden 変化 ⟺ bump」が MVP 期間を通じて破られていないことの追跡確認 |

**Phase 10 内の順序**: M42(実機反映)と M47/M48(配信・監視)は互いに独立で、M43(最終検分)は両方の後に置く。M47 は M34/M46 が揃った時点で走れるため、Phase 9(バランス調整)と並行して先に片付けてよい。**ADR-022 は ADR 原文で「リリース後フェーズ」に置かれているが、§6.2-5 の裁定で MVP の Phase 10 へ前倒し計上した**(自動ロールバックの重い部分ではなく、署名 + 2信号照合という**安全側の制約**が本体であるため、リリース前に入れておかないと意味を失う)。

### 条件付きタスク(②実機で不合格になった場合のみ発動)

summary §4.1 は実機不合格時に **+0.5〜1.0週**が乗ると明記している。その内訳を予めタスク化しておく。

| ID | 発動条件 | タスク | 担当 | ×3.5 | Sess |
|---|---|---|---|---|---|
| M42a | **#2 のヒープ増分が予算 48MB を超える**(裁定 N13 の判断が「必要」側に倒れる) | ADR-029 の**可変ドラフト in-place 更新**を実装。現在 `catchUp.ts` は呼ぶと例外を投げる二系統切替点として用意済み | **Opus** | 455k | 0.23 |
| M42b | **#1 の B4(240 DOM mount)が予算 200ms を超える** | **DOM 仮想化**(ADR 692行の Fallback)。デスクトップ実測 1.747ms = 予算の 0.87% のため発動見込みは低い(summary §3.2-2) | Sonnet | 315k | 0.16 |

---

## 3. 裁定(B/D/N)の組込み位置

裁定済み項目が「どのタスクで実際に効くか」の対応表。**裁定を反映し忘れると後から golden 全再生成 + algoVersion bump が発生する**ものを太字にした。

| 裁定 | 内容 | 組込み先 |
|---|---|---|
| **B1** | GDD 11.7 に想起困難の段22(回復)/段24(抽選)を正式追記。既存挙動の正本化ゆえ golden 37本不変 | **M13**(検収条件で「不変」を確認する) |
| **B2〜B4** | セーブのエンベロープ化 / `eventQueueSnapshot` 非保持 / `rngState` 空省略 | **M3**(M4 で往復検証) |
| **B5** | footprint 2×1 / 2×2 は GDD 仕様を維持し、**格子UI 2週の行で state 拡張を実装** | **M16**(Phase 3 の先頭に置いた) |
| **B6** | 隣接効果の正本英字ID `forgeYield`/`efficiency`/`foodYield`(未実装3種 `health`/`codifySpeed`/`defense` は reject 側) | M5・M17・**M23** |
| **B7** | 距離帯の正本 `near`/`far`/`deep` | M21・**M23** |
| **B8** | ステータス正本 `vigor`/`dexterity`/`intellect`/`fortitude`/`will` + 派生値 `combatPower` | M7・**M23** |
| **B9 + #5** | 判定単位は**盤面平均**。住民系完了時に **#5 の本判定を再計測する義務**が発生(summary §2.2) | **M14 を独立タスクとして明示**(M13 の付随作業にしない) |
| D1〜D6 | 実機判定の前提(450ms=512KB / 1100ms=cold / `idbFirstTouchMs` は補助 / GCポーズは 0.83ms 代替 / iOS ヒープは間接 / WebKit は実 Safari 非代替) | M4・M35・**M42** |
| D7 | 校正カーネルは実装せず実機直接計測で代替 | M42(カーネル実装タスクを立てない根拠) |
| N1 | `p_step = p_day × coarse/1440` の線形按分 | M13 |
| N2 / N8 / N10 | ±60% クランプは engine 定数 / tagMatrix キーは `tag\|tag` のみ / 辞書順選抜は決定論のため維持 | M17 |
| **N7** | 「学芸3連接 → 成文化+30%」は**タグペア行列で表現不能**。成文化実装段で**モデル自体の見直しが必要** | **M6 にモデル見直しの判断を含める**(対象外維持 or 集合カウント型へ拡張。後者なら ADR/GDD 改訂 + algoVersion bump) |
| N12 | mastery 上限 0.20 の相殺は MVP 現状維持、バランス調整で再評価 | M13(実装)・**M40/M41**(再評価) |
| N13 | ADR-029 可変ドラフトの要否は #2 実機ヒープ後 | **M42**(必要なら M42a 発動) |
| N14 | 失効 PAT の削除 | **ロードマップ外**(ユーザー自身の GitHub 操作) |
| W1〜W4 | 手順書・オーサリング規則・tags-spec 残件 | **M1** |
| conformance 維持 | entity 数増加時のシナリオ/ベクタ拡張と **algoVersion 規律**(ADR-016) | **M2 で規律を設計 → M10/M15/M20/M25 で実行 → M43 で確定** |
| **§6.2-1〜5**(2026-07-27裁定) | 未計上だった運営基盤 5 件を **MVP 内へタスク化** | **M47**(ADR-031)・**M46**(ADR-015)・**M44**(ADR-021)・**M45**(ADR-030)・**M48**(ADR-022) |
| **§6.2-6〜7**(2026-07-27裁定) | **ADR-025**(algoVersion sunset)と**オーサリングツール本体**は **MVP 外**を確定 | タスク化しない。発動条件・再検討条件を §6.2 に明記 |

---

## 4. クリティカルパスと並行性

### 4.1 依存グラフ

```
M44 → M45 ────────────────────────────────┐  (運営基盤列)
M1 ─┐                                      │
M2 ─┼→ M3 → M4 ──────────────────────────┐ │
    │                                      │ │
    ├→ M5 ┬→ M6 ┬→ M7 → M11 → M12 → M13 ┬─┼─┼→ M14
    │      │      ├→ M9 ──────────────┐   │ │ │
    │      │      └→ M31(UI)          │   │ │ ├→ M21 → M22 → M23 ┬→ M24
    │      ├→ M8 → M29 ┬→ M30 ┬→ M33 ─┤   │ │ │                   │
    │      │            ├→ M31 │       │   │ │ ├→ M28             │
    │      │            ├→ M32 │       │   │ │ └→ M26 → M27       │
    │      │            └→ M34 └→ M35 ─┤   │ │                     │
    │      └→ M16 → M17 ┬→ M18 → M19 ──┤   │ │                     │
    │                    └→ M26         │   │ │                     │
    └→ M10 ┬→ M15 → M20 → M25 ──────────┘   │ │                     │
            └→ M46 ─────────────────────────┘ │                     │
                 │                             │                     │
                 └──→ M36 → M37 → M38 → M39 → M40 → M41 ←───────────┘
                                                        ↓
                    ②実機(ユーザー・期間不定) → M42 → M43
                                        M34,M46 → M47 → M48
```

### 4.2 律速列(クリティカルパス)

**M5 → M6 → M7 → M11 → M13 → M21 → M22 → M23 → M36 → M37 → M38 → M39 → M40 → M41 → M43**

合計 **8,210k ≒ 4.1 セッション相当**(全体 20,425k の **40%**)。

構造は「**engine 全系統化 → 住民系 → 探索/event → sim bot → バランス調整**」の一本道である。バランス調整(M39〜M41)は夜間ゲート(M38)が全 assert を持たないと着手できず、その M38 は 5戦略bot(M36)と敵対bot(M37)を要求し、その bot は想起リスク判断(M13)と探索(M21〜M23)を要求する。**したがって住民系と探索を後回しにすると最後のバランス工程がまるごと押し出される。**

**[2026-07-27裁定] 追加した運営基盤 5 件は律速列に乗らない。** M44 → M45 → M46 → M47 → M48 は合計 1,280k(0.64 セッション)で、律速列へは **M46 → M38 の 1 点だけで合流**する。M44〜M46 を Phase 0〜1 のうちに消化しておけば M38 を待たせない。M47/M48 は M34(PWA/ビルド)と M46 が揃えば走れるので、最長でも M5 → M8 → M29 → M34 → M47 → M48 = 3,000k で、律速列の 8,210k には遠く届かない。

### 4.3 並行投入できる束

**重要な前提: 並行投入はトークン総量を減らさない**(むしろ統率オーバーヘッドで微増する)。律速が週次利用上限である以上(summary §4.4)、並行の価値は「1セッションの 5 時間枠を使い切る」ことにあり、総セッション数の削減ではない。先行フェーズでは 3 並行(裁定 Opus + T8/T9 Sonnet = 888k)を実施し、`package.json` 共有は「Edit 限定 + 直前再読」で衝突ゼロだった(MEMORY 2026-07-26)。

| 束 | タスク | 律速列との関係 |
|---|---|---|
| **A**(セーブ列) | M3 → M4 | M5 以降と完全並行可 |
| **B**(格子列) | M16 → M17 → M18 → M19 | M11〜M14(住民列)と並行可 |
| **C**(UI列) | M8 → M29 → M30/M31/M32/M33 → M34 → M35 | M8 が M5 直後に開始でき、Phase 4 以降とほぼ全域で並行可。**最も長い非律速列**(2,915k ≒ 1.5 セッション) |
| **D**(conformance列) | M10 → M15 → M20 → M25 | 各 Phase の直後に薄く挿入。単独では走らせられない(先行タスクの成果を要求する) |
| **E**(文書列) | M1・M2 | 最初に単独で消化する。M2 は M3 以降すべての前提 |
| **F**(独立) | M9・M24・M26・M27・M28 | 前提さえ揃えば任意のタイミング |
| **G**(運営基盤列)**[2026-07-27裁定・追加]** | M44 → M45 → M46 → M47 → M48 | engine 列と全域で並行可(触るのは `.github/workflows/` `wrangler.toml` `CODEOWNERS` と運営スクリプトのみで、`src/` `schema/` `content/` に一切触れない = **最も衝突しにくい束**)。**M46 で M38 の前提として 1 点だけ合流する** |

**1 セッションあたりの詰め方の目安**: 平均タスクが 426k(20,425k ÷ 48)なので、**1 セッション(2M)= 3〜5 タスク**。異なる束から選んでファイル衝突を避ける。束 G は他のどの束ともファイルが重ならないため、**並行投入の埋め草として最も安全**である。

### 4.4 ②実機計測の合流点

②実機計測はユーザー実施・**期間不定**(summary §4.2 の新規行・§5.1)。合流点は 2 つに限られる。

| 合流点 | 何が変わるか | 実機を待たずに進める方法 |
|---|---|---|
| **M19 の既定ズーム倍率** | #9b の `minLegibleSizePx` が tags-spec §9-1 の既定ズームを決める(summary §4.2 の格子UI行が明記) | tags-spec の暫定値を**定数1箇所**に隔離して実装する。#9b 到着後は定数の差し替えのみで済み、UI 構造は手戻りしない(M19 の検収条件に入れた) |
| **M42(判定の確定)** | #1/#2/#8 の合否と、裁定 N13(可変ドラフト要否)の確定 | M42 以外の全タスク(M1〜M41)は実機結果を待たずに進行できる |

**実機不合格時に手戻りするタスク**は次の 4 つに限定される。それ以外は影響を受けない。

| 不合格項目 | 手戻り先 | 追加タスク |
|---|---|---|
| #2 ヒープ増分 > 48MB | catch-up 経路(`src/platform/catchUp.ts`)。二系統切替点が 1 箇所に隔離済みなので UI/engine への波及は無い | **M42a**(可変ドラフト実装) |
| #1 の B4 > 200ms | M18/M19(格子UIの DOM 構造) | **M42b**(DOM 仮想化) |
| #1 の B2 > 450ms | M3/M4(セーブ層)。512KB 想定(D1)での restore はデスクトップ 3.75ms | 既存タスク内で対処 |
| #1 の B3 > 250ms | M8/M29(ストアと派生値)。**そもそも M35 で取り直す前提**(summary §5.3-5) | 既存タスク内で対処 |

**実機を待たずに進められる範囲 = M42 / M43 以外の全タスク**(46/48 タスク・19,550k / 20,425k = **96%**)。実機が遅れてもロードマップは止まらない。運営基盤列(M44〜M48)も実機とは完全に独立である。

なお #10(ITP 7日)は実機入手後**最初に開始する**(待ち時間が最長・summary §5.2)。**[2026-07-28] iPhone 入手予定は当面なし → #10/#7-iOS は無期限保留**(入手時に即開始の原則は不変)。#6(段階2カスケード)は ADR-018(3) により MVP 対象外のまま。

---

## 5. セッション予算計画

### 5.1 合計

| | 値 | 根拠 |
|---|---|---|
| 素見積り合計(先行計測計画 §4.2 と同じ方法) | **5.84M** | §2 の各タスク `素` 列の総和(48 タスク) |
| ×3.5(summary §4.4-1 の運用提案) | **20.4M** | 記録7タスクの実消費比 3.58倍 |
| セッション換算(÷2M) | **10.2 セッション** | summary §4.4「1セッション ≒ 2M 実効」 |
| 反復ぶんの上乗せ +30〜50%(先行計測計画 §4.4 と同じ扱い) | **26.6〜30.6M** | バランス調整の反復・不具合修正・検分差し戻し |
| **計画値** | **13.3〜15.3 セッション ≒ 13〜15 セッション** | 26.6M / 30.6M ÷ 2M |
| ±50% の幅(先行計測計画 §4.2 が明記する見積り精度) | **7〜23 セッション** | 素見積りの精度限界 |

**[2026-07-27裁定による改訂]** §6.2 の裁定で運営基盤 5 件(M44〜M48・合計 1,280k)を MVP 内へ追加したため、改訂前の 19.1M / 12〜15 セッションから **+1.28M / +0.6 セッション**した。タスク数 43 → 48。

**[2026-07-30追記]** M49(2026-07-29 裁定・385k)+ M50/M51(2026-07-30 裁定・計 595k)の追加でタスク数 48 → **51**、合計 +980k(約 +0.5 セッション)。M50 は M49 完了時に判明した欠落の補完、M51 は M46 の正直申告残件のタスク化であり、いずれも律速列には乗らない(M50 は bump 束ね運用のため実施時期は統率者判断)。

**[2026-07-31追記]** M52(瓦礫 state 化・420k)の追加でタスク数 51 → **52**、合計 +420k(約 +0.2 セッション)。M18 完了時に「GameState に地形の概念が無く UI 側だけでは瓦礫開墾を実装できない」と判明した欠落のタスク化。律速列には乗らないが、GDD 9.1 の序盤体験(瓦礫が盤面を制約する)に直結するため M30(格子ビュー UI)より前の完了が望ましい。

**[2026-07-31追記2]** M53(ニューゲーム生成+難度シード・315k)の追加でタスク数 52 → **53**、合計 +315k(約 +0.16 セッション)。M29 完了時に「初期状態を作るタスクがどこにも無い」と判明した欠落のタスク化(台帳v5 必-1)。律速列には乗らないが、**M39(バランス調整)は M53 完了後に着手**(校正が仮初期状態に依存しないため)。

**[2026-08-01追記]** 台帳v8/v9 裁定により M54(UI 残件束ね・315k・新設)+ M57(オンボーディング・315k・新設)を追加、M55/M56(UX 改修 束A/束B・計 980k)は実施済みの事後記録。タスク数 53 → **57**、新規計画分 +630k(約 +0.32 セッション)。いずれも律速列には乗らない(M54/M57 は UI のみ・M55/M56 は完了済み)。

**この数値の限界を正直に開示する。**

1. **ボトムアップ(規模ベース)の積算であり、週数からの外挿ではない。** 残り 16.1〜17.5週 を先行フェーズ(0.5週 / 外挿実消費 7.1〜7.6M)から線形外挿すると 200M オーダー(約100セッション)になるが、summary §4.4 はこの外挿を**明示的に採用しない**と結論している(先行フェーズは設計判断密度が突出して高く、UI 12画面やバランス調整はトークン単価が構造的に低いため)。本書は summary の結論に従いボトムアップを採るが、**両者は約8倍乖離している**。真値は実測でしか埋まらない。
2. **整合性チェック**: 本書のタスク実装行数の総和は約 21,050行。先行フェーズの実装行数は約 11,656行(engine+platform+schema+sim+conformance+e2e)で外挿実消費 7.1〜7.6M。行数比 1.81倍を掛けると 12.8〜13.7M となり、本書の 20.4M(反復前)は**それを上回る保守側**にある。ただし先行フェーズの 7.1〜7.6M 自体が外挿値であって実測値ではない(記録は 16 タスク中 7 タスクのみ・summary §4.4)。
3. **#12 再測定による裏づけが 1 点だけ入った。** 再測定セッションは tech/facility/event 各 1 本 + 報告書 + テスト追記で **実消費約 180k**(summary §1.1 / `authoring-times-retest-2026-07-27.md`)。報告書ぶんの固定費込みで約 60k/本となり、本書の M23(event 10本 = 580k → 58k/本)と**同オーダーで整合する**。content 系タスクの見積り軸としてはこれが唯一の実測アンカーである。
3. **上振れリスクの方が構造的に大きい**。先行フェーズの実消費比は 2.04〜7.52倍とばらつき、最悪ケース(T7後半 = 実行方式の詰め)は 7.5倍だった。3.5 は平均であって上限ではない。

### 5.2 セッション運用

- 予算単位は**トークンではなくセッション(5時間枠 ≒ 2M)**(summary §4.4-2)。
- 1 セッションに **3〜5 タスク**を、異なる束(§4.3)から選んで並行投入する。同一ファイルを触るタスクは同一セッションに入れない。`package.json` 等の共有ファイルは「Edit 限定 + 直前再読」を指示する(MEMORY 2026-07-26 の実績)。
- **Fable5 はセッションの冒頭(指示出し)と末尾(検分)のみに現れる。** 実装中の会話に入ると統率コストがトークンを食う。
- セッションの区切りでは MEMORY.md と RAG を更新する(CLAUDE.md のルール)。
- 律速列(§4.2)のタスクは**セッションの先頭に置く**。枠切れで中断した場合、非律速列の未完了は次セッションへ持ち越せるが、律速列の中断は全体を押し出す。

### 5.3 MEMORY.md 記録規律(必須)

先行フェーズは 16 タスク中 7 タスクしか実消費を記録せず、**#11(週次セッション消費量)の代替データが不完全になった**(summary §4.4)。同じ失敗を繰り返さないため、**各タスク完了時に以下を MEMORY.md へ 1 行で記録することを完了条件に含める**(§0.4 の共通検収条件)。

```
[YYYY-MM-DD] MX完了(担当モデル・コミットハッシュ、Fable5検分: テスト件数): 見積りXXXk / 実消費YYYk / 比Z.ZZ倍 — 要点1〜3行
```

- 記録が無いタスクは**完了と見なさない**。
- **5 タスクごとに実績比を再計算し、§5.1 の係数 3.5 を実測値へ更新する**。更新したら本書 §5.1 を改訂する(本書は生きた文書として扱う)。
- セッション終了時には「そのセッションで消費した総トークン / 5時間枠の消費率」も記録する(1セッション ≒ 2M という前提そのものの検証データになる)。

---

## 6. 見積り前提の確定記録と残余リスク

### 6.1 #12 再測定(content 単価)— **[2026-07-27] プレースホルダ解消**

本書の初稿時点では #12 は部分達成(wall-clock が unmeasured)で、M23 の見積りをプレースホルダとしていた。**再測定が完了し #12 は合格へ更新された**(summary §2.5 末尾 / 一次データ `docs/measurements/authoring-times-retest-2026-07-27.md`)。

- **実測(正味 wall-clock・参照読了→pass確定)**: tech `techSimpleBedding` **56.162秒** / facility `reservoir` **33.715秒** / event `eventNearOldCistern` **56.769秒**。**reject 0回 / 3カテゴリ**を、手順書のみを渡された別セッション(順序効果なし条件)で再現。
- **判定**: 56〜150h レンジは人間実施前提の見積りであり、LLM 実施の実測はレンジ下限を**約3桁**下回る。よって **content 単価の律速は wall-clock ではなくトークン/セッション枠**であると確定した(summary §2.5)。**これは本書 §5 の予算軸そのものであり、M23 の見積り軸(トークン)は正しかったことになる。**
- **M23 の扱い**: 見積り 580k / 0.29 Sess を**据え置きで確定**する。裏づけは §5.1-3 のクロスチェック(再測定セッション実消費 180k / 3本 ≒ 60k/本 vs M23 の 58k/本)。ただしこれは n=1 のアンカーであり、M23 完了時の実消費で再検証すること。
- **据え置きの注意**: event のスキーマ構築(487行 + テスト22件)は**1回限りの基盤投資で完了済み**であり、10本に按分してはならない(summary §2.5)。単純平均すると単価を過大評価する。
- GDD 13.2 の「プロトタイプ計測後に全体スケジュールを再計算するチェックポイント」は、#12 再測定と本書 §5 で**消化済み**(summary §5.3-3)。
- **残るのは reject 再試行コストの分布**(reject が 1 度も起きていないため標本が無い)。③#11 の本計測へ持ち越し(summary §1.3)。これは §6.2-7 のオーサリングツール判断とも連動する。

### 6.2 未計上作業の裁定 — **[2026-07-27裁定・確定]**

以下は ADR に決定があるが **GDD 13.1 にも summary §4.2 にも行が存在しなかった**もの。本書初稿では「要ユーザー判断」として列挙するに留めたが、**2026-07-27 にユーザー裁定により 5 件を MVP 内へタスク化・2 件を MVP 外へ確定した。**

| # | 対象 | 内容 | **裁定(2026-07-27)** |
|---|---|---|---|
| 1 | **ADR-031** | stable/beta 昇格機構(soak N=3日・手動 promotion)。ADR 自身が「MVP へ前倒し」と明記しているのに工数行が無かった = **実質的な抜け** | **MVP 内へタスク化 → M47**(Phase 10)。`wrangler.toml` の `[env.stable]`/`[env.beta]` による配信基盤設定を含む |
| 2 | **ADR-015** | guardrail(symlink/非通常ファイル reject + 正準パイプライン整合)。週次 content 運営の前提 | **MVP 内へタスク化 → M46**(Phase 8 の先頭・**M38 の前提**として配置) |
| 3 | **ADR-021** | シークレット/課金の壁の preflight。金銭安全の根幹 | **MVP 内へタスク化 → M44**(Phase 0・小タスク) |
| 4 | **ADR-030** | ソロ運営のレビュー分離(CODEOWNERS デッドロック解消)。週次 content 運営の前提 | **MVP 内へタスク化 → M45**(Phase 0・小タスク) |
| 5 | **ADR-022** | リリース後監視(署名付きヘルスビーコン・2信号照合) | **MVP 内へタスク化 → M48**(Phase 10)。ADR 原文は「リリース後フェーズ」だが、**リリース時に必要**との裁定により Phase 10 へ前倒し計上 |
| 6 | **ADR-025** | algoVersion sunset(凍結アーカイブ・ライブレジストリ 3 世代ハード上限) | **MVP 外を確定。** 退役版が存在しない段階では実装対象が無い。**発動条件 = 初の algoVersion bump 後に旧版の退役が実際に発生した時点**(= 少なくとも M43 の algoVersion 確定より後)。それまでは着手しない |
| 7 | **オーサリングツール本体** | W3 派生。6桁 floor 規則の組込み先。M1 は規則の文書化のみでツール実装は未計上だった | **MVP 外を確定。** #12 再測定の実測(手順書のみを渡された別セッションで **reject 0回 / 3カテゴリ**・正味 34〜57秒/本 = 約1分/本)により、**手順書 + スタンドアロン検証器で代替可能**と実証された(§6.1・`authoring-times-retest-2026-07-27.md`)。ツール化の投資対効果が現時点で成立しない。**再検討条件 = reject 分布が問題化した場合**(③#11 の本計測で reject 再試行コストが観測されたとき) |

**この裁定による本書への影響**: タスク 43 → **48**、合計 19.1M → **20.4M**、計画値 12〜15 → **13〜15 セッション**(§5.1)。クリティカルパスは**不変**(追加 5 件はいずれも律速列に乗らない・§4.2)。週数(summary §4.3 の 16.1〜17.5週)は出典が無いため更新していない(§1 末尾)。

### 6.3 本ロードマップ自体の残余リスク

1. **バランス調整(M39〜M41)の反復回数が読めない。** GDD 13.1 が「各段階の追加工数上限を先に設定」と指定しているのはこのためであり、上限設定はユーザー承認事項として残る。
2. **UI 12画面の 2.2週 → 2,915k という換算に実績の裏付けが無い。** 先行フェーズに UI 実装が 1 つも含まれないため、UI のトークン単価は本ロードマップ内で初めて測られる。M29 完了時点で係数を見直すこと。
3. **M6 の N7 判断(成文化のモデル見直し)が「拡張する」側に倒れると、tagMatrix の構造そのものが変わり algoVersion bump + golden 全再生成が発生する。** 現時点の裁定は MVP 対象外の維持だが、実装段で再燃しうる。
4. **conformance 維持のトークン配分(本書で 1,910k = 全体の 9%)は summary §4.2 の週数配分(0.5〜0.8週 / 16.1〜17.5週 = 3〜5%)より厚い。** 週数とトークンの比率が一定でないことの表れであり、どちらが正しいかは実測でしか決まらない。
5. **[2026-07-27裁定・追加] 運営基盤 5 件(M44〜M48・1,280k)の見積り根拠は ADR の記述量と類似既存タスクとの比較のみである。** 先行フェーズで実施した CI 系タスクは T15(`calibrate.yml`・1ファイル80行・素40k)1 件だけで、しかも `npm ci` の optional 依存問題で**3 コミットを消費した**(MEMORY 2026-07-26)。**CI / wrangler / GitHub 設定は「1 発で通らない」性質**があり、これら 5 件は他のタスクより上振れしやすい。M44 完了時点で係数を見直すこと。
6. **[2026-07-27裁定・追加] M47 は本書で唯一「まだ 1 行も存在しない基盤」に触れるタスクである。** `wrangler.toml` はリポジトリに存在せず、Workers 静的アセット配信は ADR-025/ADR-031 の文言上の決定(2026-07-25 改訂)としてしか存在しない。実デプロイで初めて判明する制約(サブドメイン取得・assets binding の SPA モード挙動・無料枠)がありうる。

### 6.4 記録媒体システムの織込み — **[2026-07-27追補・ユーザー承認済み]**

GDD 11.1 [2026-07-27追補] の記録媒体システム(石板/紙)を**最小構成**で本ロードマップへ織り込んだ。新規タスクは追加しない:

| 吸収先 | 追加内容 |
|---|---|
| M6 | 媒体2種(enum固定)・媒体別コスト/時間・媒体別記録の並存・balance `recordMedia` ブロック |
| M22 | event 効果プリミティブ `destroyRecords{medium, scope}`(content 側未使用のまま conformance で固定) |
| M27 | おまかせ成文化に規則1つ追加「唯一保持 tech → 石板、他 → 紙」(GDD 追補に明記済み・行変更なし) |
| M28 / M33 | ナップサックの媒体別重み(石板 1.0/紙 0.25)。GDD 10.2 追補経由で仕様が流入するため行変更なし |
| M31 | ⑥成文化キューの媒体トグル。同上 |

**見積り影響: 合計 +0.1 Sess 未満**(各タスクの規模レンジ内で吸収・表の数値は据え置き)。**火災イベントは MVP 出荷ゼロ**であり、MVP 後に additive な event JSON のみで解禁できる(engine 変更なし・bump 不要)ことが本織込みの成立条件である。書庫施設・熱源隣接火災リスク・輸送中喪失は MVP 対象外(GDD 追補に記録)。

---

## 7. 完了条件(MVP の Done)

| # | 条件 | 検証手段 |
|---|---|---|
| 1 | GDD 13.4「MVP に入れる物」の全項目が実装されている | 項目ごとのタスク対応表(§1)で消込 |
| 2 | `npm test` / `typecheck` / `lint` / `format` / `golden:check` / `conformance:e2e` / `measure:contrast` が全 pass | CI |
| 3 | 夜間ゲートの GDD 11.4 全 11 条件が assert として存在し pass する | M38 の成果物 |
| 4 | 敵対 bot 6種(GDD 11.6)が毎晩必須ゲートとして走る | M37/M38 |
| 5 | #5 の**本判定**が住民系実装後に再計測され、盤面平均で 1〜3回/住民/週に収まる(裁定 B9) | M14 |
| 6 | ②実機計測 5項目の判定が付き、結果 JSON が `docs/measurements/` に永続化されている | M42 |
| 7 | algoVersion が確定し、ADR-016 の「golden 変化 ⟺ bump」が MVP 期間を通じて破られていない | M43 |
| 8 | 全 **48** タスクの実消費が MEMORY.md に記録されている(§5.3) | MEMORY.md の grep |
| 9 | GDD 13.2 のチェックポイント(#12 再測定に基づく全体スケジュール再計算)が閉じている | **[2026-07-27 達成]** §6.1(再測定完了・summary §2.5 で #12 合格) |
| 10 | **[2026-07-27裁定・追加] 週次 content 運営が成立する**: preflight が `ANTHROPIC_API_KEY` を検出して起動を止め(ADR-021)、bot 作成 PR + human 承認で admin bypass なしにブランチ保護が成立し(ADR-030)、正準パイプライン 10 段が CI 記述と一致している(ADR-015) | M44・M45・M46 |
| 11 | **[2026-07-27裁定・追加] 配信と監視が成立する**: `[env.stable]`/`[env.beta]` が別 Worker として出せ、soak N=3日 未満の promotion を機械判定で block でき(ADR-031)、ヘルスビーコンが署名付きかつ**単独ではロールバックを起動しない**(ADR-022) | M47・M48 |
